# Frontend (Vite) — used at build time
# For local dev, omit this and the app uses http://localhost:8000
# For production (e.g. Vercel), set in your host's env and redeploy:
VITE_API_URL=https://your-backend-url.run.app
